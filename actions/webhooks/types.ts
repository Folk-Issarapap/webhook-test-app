/**
 * Re-exports for server actions once implemented.
 * Prefer importing domain types from `@/schemas/webhook`.
 */
export type {
  WebhookEndpointDisplay,
  WebhookEndpointRow,
  WebhookEndpointTokenParts,
  WebhookRequestRow,
} from "@/schemas/webhook";
